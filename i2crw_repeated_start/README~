This app reads from / writes to i2c slave devices connected to dm365.

To compile:
arm-arago-linux-gnueabi-gcc *.c -o i2crs

To run on target (dm)
./i2crs <slave_address> <0: write, 1: read> <register_address> <value to be written(optional)
